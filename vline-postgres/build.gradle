dependencies {
    api(project(":vline-core"))

    api libs.get('druid')
    api libs.get('postgres')
    api libs.get('anyline-mvc')
    api libs.get('anyline-environment')
    api libs.get('anyline-data-jdbc-postgres')
    api libs.get('flyway-core')
    api libs.get('flyway-postgres')

    api 'org.springframework.boot:spring-boot-starter-jdbc'
    api 'org.springframework.boot:spring-boot-starter-web'
}
